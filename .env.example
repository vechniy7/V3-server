PORT=3000
REDIS_PREFIX=nexus:v3

# Upstash Redis (https://upstash.com) — set in Render Environment
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Admin panel + API (Bearer token). Generate a long random string.
ADMIN_RESET_TOKEN=
